<script lang="ts">
	import FlatResourceManager from '$lib/components/FlatResourceManager.svelte';
</script>

<FlatResourceManager
	kind="saver"
	label="Savers"
	lede="Where a measurement's rows go — one row per integration, tagged with every parameter set at the time."
/>
