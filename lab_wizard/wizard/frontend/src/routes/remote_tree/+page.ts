// Servers are discovered at mount rather than at load, so navigating here after
// starting a server elsewhere shows it without a full reload.
export const prerender = true;
export const ssr = false;
